// Finding length of a string without using library functions.

#include <stdio.h>
int main()
{
    char str[] = "hello";
    int length;

    for (length = 0; str[length] != '\0'; length++)
    {
        
    }
    printf("Length of string:%d", length);
    return 0;
}
